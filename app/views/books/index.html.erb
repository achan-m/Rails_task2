<%= render 'list', books: @books %>

<div class="col-lg-8">
    <table class="table table-striped">
      <thead>
        <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @books.each do |book| %>
         <tr>
           <td>投稿ユーザー画像</td>
           <td><%= book.title %></td>
           <td><%= book.body %></td>
         </tr>
        <% end %>
      </tbody>
    </table>
    </div>

  </div>
</div>